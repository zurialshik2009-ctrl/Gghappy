name: Test GDrive Upload (PyDrive2)
on:
  workflow_dispatch:

jobs:
  test-upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: pip install PyDrive2 google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: Create Test File
        run: echo "Stability Test using PyDrive2 - $(date)" > test.txt

      - name: Upload using PyDrive2
        env:
          GDRIVE_CREDENTIALS: ${{ secrets.GDRIVE_CREDENTIALS_DATA }}
          FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
        run: |
          python - <<EOF
          import os
          import json
          import sys
          from pydrive2.auth import GoogleAuth
          from pydrive2.drive import GoogleDrive
          from google.oauth2 import service_account

          try:
              creds_json = json.loads(os.environ['GDRIVE_CREDENTIALS'])
              folder_id = os.environ['FOLDER_ID']
              
              # Create temporary credentials file
              with open('service_key.json', 'w') as f:
                  json.dump(creds_json, f)

              gauth = GoogleAuth()
              scope = ['https://www.googleapis.com/auth/drive']
              gauth.auth_method = 'service'
              gauth.credentials = service_account.Credentials.from_service_account_file(
                  'service_key.json', scopes=scope)
              
              drive = GoogleDrive(gauth)

              print("Attempting to upload to Google Drive using PyDrive2...")
              
              file_params = {
                  'title': 'test.txt',
                  'parents': [{'id': folder_id}]
              }
              
              file_obj = drive.CreateFile(file_params)
              file_obj.SetContentFile('test.txt')
              
              # SupportsAllDrives ensures it respects folder permissions
              file_obj.Upload(param={'supportsAllDrives': True})
              
              print(f"✅ Test Successful! File ID: {file_obj['id']}")

          except Exception as e:
              print(f"❌ Test Failed. Error: {str(e)}")
              sys.exit(1)
          finally:
              if os.path.exists('service_key.json'):
                  os.remove('service_key.json')
          EOF
